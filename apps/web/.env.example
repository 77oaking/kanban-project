# Public URL of the API (used for fetch requests from the browser)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Public URL the Socket.io client connects to
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
